# Runtime resources

Release builds place platform FFmpeg and FFprobe executables under:

- `bin/windows-x64/`
- `bin/linux-x64/`

The binaries are downloaded during native build workflows and are intentionally not committed to Git.
